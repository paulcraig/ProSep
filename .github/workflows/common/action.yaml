name: python-setup
description: Common setup for python
runs:
  using: "composite"
  steps:
    - name: Checkout Repository
      uses: actions/checkout@v4.2.2
    - name: Setup python
      uses: actions/setup-python@v5.3.0
      with:
        python-version: '3.12'