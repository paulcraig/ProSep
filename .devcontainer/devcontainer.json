{
	"name": "Python and NodeJS",
	"build": {
		"context": "..",
		"dockerfile": "Dockerfile"
	},
	"overrideCommand": true,
	"customizations": {
		"vscode": {
			"extensions": [
				"ms-python.python",
				"dbaeumer.vscode-eslint",
				"esbenp.prettier-vscode"
			]
		}
	},
	"forwardPorts": [3000, 8000],
	"postCreateCommand": "python -m venv .venv && .venv/bin/pip install -r requirements.txt && npm --prefix frontend/jbio-app ci"
}
